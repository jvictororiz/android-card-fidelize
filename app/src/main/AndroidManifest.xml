<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.rorizinfo.cardFidelize">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="BaseApplication"
        android:theme="@style/Theme.Androidcardfidelize">
        <activity
            android:windowSoftInputMode="adjustResize"
            android:name="br.com.rorizinfo.cardFidelize.ui.features.authentication.AuthenticationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="br.com.rorizinfo.cardFidelize.ui.features.home.user.HomeUserActivity" />
        <activity android:name=".ui.features.home.company.HomeCompanyActivity" />
    </application>

</manifest>